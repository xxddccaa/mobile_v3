# Mobile Agent v3 工作流程详解

## 📋 概述

Mobile Agent v3 是一个**多智能体协作系统**，通过多个 AI 角色（Manager、Operator、Reflector）协作来完成手机操作任务。整个系统采用**"观察-计划-执行-反思"**的循环模式。

---

## 🏗️ 系统架构

### 核心组件

1. **InfoPool（信息池）**：存储所有中间状态和数据
2. **Manager（规划者）**：负责制定计划和跟踪进度
3. **Executor（操作者）**：负责执行具体操作
4. **ActionReflector（反思者）**：评估操作是否成功
5. **Notetaker（记录者）**：可选，记录重要信息

### 执行流程

```
┌─────────────────────────────────────────────────────────┐
│                   用户输入任务                           │
│          "帮我整理所有与李荣浩相关的微博动态"            │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  步骤 1: 截图获取                                        │
│  - 通过 ADB/uiautomator2 截取手机屏幕                   │
│  - 保存截图到本地 images/ 目录                          │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  步骤 2: Manager（规划者）分析                          │
│  - 查看当前屏幕截图                                      │
│  - 分析用户任务                                          │
│  - 制定/更新执行计划                                     │
│  - 输出: 完成的目标 + 下一步计划                        │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  步骤 3: Operator（操作者）执行                         │
│  - 查看当前屏幕截图                                      │
│  - 根据计划选择具体操作                                  │
│  - 输出: 操作类型 + 坐标/文本                            │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  步骤 4: 执行操作                                        │
│  - 通过 Controller 执行操作（点击/输入/滑动等）         │
│  - 等待界面响应（2-8秒）                                 │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  步骤 5: 再次截图（操作后状态）                          │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
┌─────────────────────────────────────────────────────────┐
│  步骤 6: ActionReflector（反思者）评估                  │
│  - 对比操作前后的两张截图                                │
│  - 判断操作是否成功（A/B/C）                             │
│  - 更新进度状态                                          │
└──────────────────┬──────────────────────────────────────┘
                   │
                   ▼
        ┌──────────────────────┐
        │   任务完成了吗？      │
        └──┬───────────────┬───┘
           │ 是            │ 否
           ▼               ▼
      保存结果         ┌─────────┐
      退出流程         │ 返回步骤1│
                      └─────────┘
```

---

## 📝 日志解析（以你的任务为例）

### 任务：整理所有与李荣浩相关的微博动态

### Step 1: Manager 规划阶段

```
### Manager ... ###

Completed subgoal: No completed subgoal.
Planning thought: 用户请求整理所有与李荣浩相关的微博动态。根据提供的指南，我需要打开微博应用并进行一系列操作...
Plan: 1. 打开微博应用：执行 `open_app` 动作。 2. 点击发现按钮。 3. 输入搜索词"李荣浩"...
```

**解释**：
- Manager 看到初始屏幕（手机主界面）
- 分析任务：需要搜索"李荣浩"的微博
- 制定计划：拆分成 9 个子步骤
- 记录：当前没有已完成的目标

---

### Step 2: Operator 执行阶段

```
### Operator ... ###

Thought: 根据用户请求和整体计划，第一步是打开微博应用...
Action: {"action": "open_app", "text": "微博"}
Action description: 打开微博应用。
成功打开应用: 微博 (com.sina.weibo)
```

**解释**：
- Operator 看到当前屏幕（主界面）
- 思考：第一步是打开微博应用
- 选择操作：`open_app`，应用名"微博"
- 执行：通过 Controller 调用 `controller.open_app("微博")`
- 结果：成功打开微博应用

---

### Step 3: ActionReflector 评估阶段

```
### Action Reflector ... ###

Action reflection outcome: A
Action reflection error description: None
Action reflection progress status: No completed subgoal.
```

**解释**：
- Reflector 对比**操作前**和**操作后**两张截图
- 判断结果：**A（成功）** - 操作达到预期
- 错误描述：无错误
- 进度状态：更新为"打开微博应用已完成"

---

### Step 4: 继续下一轮循环

```
### Manager ... ###

Completed subgoal: - 打开微博应用：执行 `open_app` 动作。
Planning thought: 微博应用已经成功打开，接下来需要按照计划进行操作...
Plan: 1. 点击发现按钮。 2. 输入搜索词"李荣浩"...
```

**解释**：
- Manager 看到新的屏幕（微博界面）
- 确认：第一个子目标已完成
- 更新计划：移除已完成项，继续下一步

---

## 🔍 操作类型说明

### 支持的操作（Atomic Actions）

1. **`open_app`** - 打开应用
   ```json
   {"action": "open_app", "text": "微博"}
   ```

2. **`click`** - 点击屏幕
   ```json
   {"action": "click", "coordinate": [546, 2290]}
   ```

3. **`type`** - 输入文本
   ```json
   {"action": "type", "text": "李荣浩"}
   ```

4. **`swipe`** - 滑动
   ```json
   {"action": "swipe", "coordinate": [546, 1785], "coordinate2": [546, 595]}
   ```

5. **`system_button`** - 系统按钮（返回/主页）
   ```json
   {"action": "system_button", "button": "Back"}
   ```

6. **`answer`** - 回答用户（任务完成时使用）

---

## 🎯 操作结果评估（Action Reflection）

### 结果类型

- **A（成功）**：操作达到预期效果
  ```
  Action reflection outcome: A
  Action reflection error description: None
  ```

- **B（失败 - 错误页面）**：操作导致进入错误页面，需要返回
  ```
  Action reflection outcome: B
  Action reflection error description: The last action results in a wrong page...
  ```

- **C（失败 - 无变化）**：操作没有产生任何变化
  ```
  Action reflection outcome: C
  Action reflection error description: The last action produces no changes...
  ```

### 示例：滑动失败的判断

```
Action reflection outcome: C
Action reflection error description: The last action was a swipe to scroll down the page, 
but the content displayed before and after the swipe appears to be exactly the same. 
This indicates that the swipe did not produce any changes, suggesting that the content 
has already been scrolled to the bottom...
```

**解释**：Reflector 发现滑动前后截图内容一样，判断已经到底部，无法继续滚动。

---

## 🔄 错误处理机制

### 连续错误检测

```python
# 如果最近 2 次操作都失败（B 或 C）
if len(info_pool.action_outcomes) >= err_to_manager_thresh:
    latest_outcomes = info_pool.action_outcomes[-err_to_manager_thresh:]
    count = 0
    for outcome in latest_outcomes:
        if outcome in ["B", "C"]:
            count += 1
    if count == err_to_manager_thresh:
        info_pool.error_flag_plan = True  # 标记需要重新规划
```

**解释**：
- 如果连续 2 次操作失败，系统会标记 `error_flag_plan = True`
- Manager 会收到"可能卡住"的提示，需要重新规划

---

## 📊 特殊功能：复制检测

### 你的任务中的关键代码

```python
# 检测是否点击了复制按钮
copy_keywords = ["复制", "copy", "复制按钮", "复制内容", "点击复制", "点击copy"]
action_desc_lower = action_description.lower()
is_copy_action = any(keyword.lower() in action_desc_lower or keyword in action_description for keyword in copy_keywords)

if is_copy_action:
    # 等待复制操作完成
    time.sleep(1)
    
    # 获取粘贴板内容
    clipboard_content = controller.get_clipboard()
    
    if clipboard_content:
        print("检测到复制操作，粘贴板内容如下：")
        print(clipboard_content)
        
        # 保存到 task_result.json
        task_result_data = {
            "goal": instruction,
            "finish_dtime": formatted_time,
            "hit_step_limit": 0.0,
            "clipboard_content": clipboard_content
        }
        
        # 停止操作
        break
```

**解释**：
- 当 Operator 执行的操作描述中包含"复制"关键词时
- 系统会自动检测并获取粘贴板内容
- 保存内容到 `task_result.json`
- 然后停止整个流程

---

## 📁 日志文件结构

```
logs/
└── 20251030_164630_帮我整理所有与李荣浩/
    ├── images/
    │   ├── screenshot_2025-10-30-*.png  # 操作前后的截图
    ├── step_1/
    │   ├── manager.json      # Manager 的输入输出
    │   ├── operator.json     # Operator 的输入输出
    │   └── reflector.json    # Reflector 的输入输出
    ├── step_2/
    │   └── ...
    └── task_result.json      # 最终结果（包含粘贴板内容）
```

---

## 🎨 关键设计思想

### 1. **多智能体协作**
- Manager：高层次的规划
- Operator：低层次的操作
- Reflector：独立的评估反馈

### 2. **视觉理解**
- 每一步都依赖截图进行视觉理解
- 使用多模态 LLM（如 Qwen-VL）理解屏幕内容

### 3. **自反馈机制**
- 每次操作后都会评估是否成功
- 失败时自动调整策略

### 4. **渐进式规划**
- 不是一次性规划所有步骤
- 根据当前状态动态调整计划

---

## 💡 你的日志中的完整流程

1. **打开微博** → Manager 规划 → Operator 执行 `open_app` → Reflector 评估成功（A）

2. **点击发现** → Manager 更新计划 → Operator 执行 `click[546, 2290]` → Reflector 评估成功（A）

3. **点击搜索框** → Operator 执行 `click[358, 184]` → Reflector 评估成功（A）

4. **输入"李荣浩"** → Operator 执行 `type("李荣浩")` → 使用 uiautomator2 输入中文 → Reflector 评估成功（A）

5. **点击搜索按钮** → Operator 执行 `click[930, 345]` → Reflector 评估成功（A）

6. **点击智搜按钮** → Operator 执行 `click[273, 314]` → Reflector 评估成功（A）

7. **点击继续问智搜** → Operator 执行 `click[240, 2245]` → Reflector 评估成功（A）

8. **滚动到底部** → Operator 执行 `swipe` → Reflector 评估失败（C，已到底部）

9. **点击复制按钮** → Operator 执行 `click[896, 184]` → **系统检测到复制操作** → 获取粘贴板 → 保存结果 → 停止流程

---

## 🔧 技术细节

### 坐标系统

- 默认使用**绝对坐标**（像素值）
- 如果使用相对坐标（`coor_type != "abs"`），坐标会被转换：
  ```python
  action_object['coordinate'] = [
      int(action_object['coordinate'][0] / 1000 * width), 
      int(action_object['coordinate'][1] / 1000 * height)
  ]
  ```

### 中文输入支持

- **优先使用 uiautomator2**（支持中文）
- 回退到 ADB Keyboard（需要额外安装）

### 截图管理

- 每次操作前截图：`screenshot_before.png`
- 每次操作后截图：`screenshot_after.png`
- Reflector 同时接收两张截图进行对比

---

## 📌 总结

Mobile Agent v3 通过**"观察-计划-执行-反思"**的循环，实现了：
- ✅ 视觉理解手机界面
- ✅ 智能规划任务步骤
- ✅ 自动执行操作
- ✅ 自我评估和纠错
- ✅ 完整的日志记录

整个系统就像一个**眼睛能看到、手能操作、大脑会思考**的智能助手！

